services:
  service1:
    image: nginx:latest
    ports:
    - 1026:80
    volumes:
    - ./conf/nginx.conf:/etc/nginx/nginx.conf:ro
    - ./data/:/www/data/:ro
  service2:
    image: nginx:latest
    ports:
    - 1029:80
    volumes:
    - ./conf/nginx.conf:/etc/nginx/nginx.conf:ro
    - ./data2/:/www/data/:ro
version: '3.8'
